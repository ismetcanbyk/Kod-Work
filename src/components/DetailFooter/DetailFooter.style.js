import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        position: "relative",
        backgroundColor: "white",
    },
    buttonContainer: {
        flexDirection: "row",
    }
});